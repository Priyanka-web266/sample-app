us-west1-docker.pkg.dev/PROJECT_ID/my-repository/hello-cloudbuild-dev:v1.0'
gcloud config get-value project
us-west1-docker.pkg.dev/your-project-id/my-repository/hello-cloudbuild-dev:v1.0
apiVersion: apps/v1
kind: Deployment
metadata:
  name: development-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: dev-app
  template:
    metadata:
      labels:
        app: dev-app
    spec:
      containers:
      - name: dev-container
        image: <todo>
        ports:
        - containerPort: 8080
